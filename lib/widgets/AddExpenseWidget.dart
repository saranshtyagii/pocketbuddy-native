import 'package:flutter/material.dart';

class Addexpensewidget extends StatelessWidget {
  final String screen;
  const Addexpensewidget({super.key, required this.screen});

  @override
  Widget build(BuildContext context) {
    return Text("Hello Words");
  }

  addExpense() {
  }

}